# SRS 直播平台架构扩容策略

## 一、概述

### 1.1 目标

支持 **500 ~ 2000 人** 并发直播观看，并具备后续 **水平扩容至万人级** 的能力。核心原则：**不改代码、不改业务逻辑，仅通过配置、部署和基础设施层面完成扩容**。

### 1.2 当前架构基线

```
┌─────────────────────────────────────────────────────┐
│                    负载均衡层                          │
│  Nginx / HAProxy (SSL Termination + 反向代理)         │
└──────────────────────┬──────────────────────────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
   ┌─────▼─────┐ ┌─────▼─────┐ ┌─────▼─────┐
   │ WS Node 1 │ │ WS Node 2 │ │ WS Node N │  ← 无状态，可水平扩展
   │ API :8080 │ │ API :8080 │ │ API :8080 │
   │ WS :8081  │ │ WS :8081  │ │ WS :8081  │
   └─────┬─────┘ └─────┬─────┘ └─────┬─────┘
         │             │             │
         └─────────────┼─────────────┘
                       │
         ┌─────────────▼─────────────┐
         │       Redis 集群           │  ← 缓存 + PubSub + 限流
         └─────────────┬─────────────┘
                       │
         ┌─────────────▼─────────────┐
         │       MySQL 主从           │  ← 持久化存储
         └───────────────────────────┘

SRS 媒体层 (独立集群，每个节点独立部署)
┌──────────┐ ┌──────────┐ ┌──────────┐
│ SRS 节点1│ │ SRS 节点2│ │ SRS 节点N│  ← 无状态，可水平扩展
└──────────┘ └──────────┘ └──────────┘
```

### 1.3 容量估算模型

**场景假设：** 1 个主播推流 + 2000 个观众拉流

| 资源项 | 单人消耗 | 500 人总计 | 2000 人总计 | 瓶颈组件 |
|--------|----------|------------|-------------|----------|
| 推流带宽 (上行) | 2.5 Mbps (主播) | 2.5 Mbps | 2.5 Mbps | SRS 接收节点 |
| 拉流带宽 (下行) | 1.5 Mbps (观众) | 750 Mbps | 3 Gbps | SRS 集群 + 网络出口 |
| WebSocket 连接 | 1 连接/人 | 501 连接 | 2001 连接 | WS Node 内存 |
| WebSocket 心跳 | 1 msg/15s/人 | 33 msg/s | 133 msg/s | WS Node CPU |
| Redis 心跳写 | 1 write/15s/人 | 33 writes/s | 133 writes/s | Redis 单节点 |
| MySQL 查询 | 按需 | 低 | 低 | 非瓶颈 |

---

## 二、0 代码改动扩容策略（核心）

### 2.1 策略总览

```
扩容策略矩阵
├── 基础设施层（零代码改动）
│   ├── 2.1 增加 SRS 节点
│   ├── 2.2 增加 WebSocket 节点
│   ├── 2.3 升级 Redis 为集群模式
│   └── 2.4 升级 MySQL 读写分离
├── 配置层（仅改配置，不改代码）
│   ├── 2.5 调优连接池与线程池
│   ├── 2.6 调优心跳扫描间隔
│   └── 2.7 调优限流阈值
└── 架构层（不改逻辑，仅加组件）
    ├── 2.8 引入消息队列削峰
    └── 2.9 引入 CDN 回源降本
```

### 2.2 增加 SRS 节点（零代码改动）

SRS 本身是无状态媒体转发节点，**只需增加实例并注册到集群管理器**，无需改动任何代码。

**操作步骤：**

```
1. 部署新 SRS 实例（使用相同配置文件）
2. 配置 SRS 节点启动时自动注册到后端集群管理器
3. 集群管理器自动发现新节点，开始分配推拉流
4. 下线：SRS 节点关闭后，集群管理器自动摘除
```

**单节点容量：**

| 规格 | 推流上限 | 拉流上限 | 内存 |
|------|----------|----------|------|
| 2C4G | 50 路 | 200 路 | 充足 |
| 4C8G | 100 路 | 500 路 | 充足 |
| 8C16G | 200 路 | 1000 路 | 充足 |

**推荐配置：** 4C8G，每节点承载 300 ~ 500 观众拉流。2000 人需要 **4 ~ 6 个 SRS 节点**。

### 2.3 增加 WebSocket 节点（零代码改动）

WebSocket 服务已设计为无状态，**以业务 uid 为唯一标识，不依赖原生 sessionId**，可直接水平扩展。

**操作步骤：**

```
1. 复制部署包到新节点
2. 修改 application.yml 中 NODE_ID 为唯一标识
3. 启动服务，节点自动注册到 Redis
4. 用户重连时自动漂移到健康节点
```

**单节点容量：**

| 规格 | WebSocket 连接上限 | 内存占用 | 说明 |
|------|-------------------|----------|------|
| 2C4G | 2000 连接 | ~1.5 GB | 适合初期 |
| 4C8G | 5000 连接 | ~3 GB | 推荐规格 |
| 8C16G | 10000 连接 | ~6 GB | 高密度部署 |

**推荐配置：** 4C8G，每节点承载 2000 ~ 3000 连接。2000 人只需 **1 个节点**（双节点容灾）。

### 2.4 Redis 集群模式升级（零代码改动）

当前 Redis 单节点在 2000 人场景下（133 writes/s）完全够用，但为后续扩容至万人级，需升级为 Redis Cluster。

**改动范围：** 仅修改 `spring.redis.cluster.nodes` 配置项，**代码零改动**（Spring Data Redis 自动适配 Cluster 模式）。

```yaml
# 单节点 → 集群，仅改配置
spring:
  redis:
    # 旧（单节点）
    # host: 192.168.1.10
    # port: 6379
    
    # 新（集群模式）
    cluster:
      nodes:
        - 192.168.1.10:6379
        - 192.168.1.11:6379
        - 192.168.1.12:6379
```

**Redis 集群容量估算：**

| 并发数 | 写 QPS | 读 QPS | 内存 | 推荐集群规格 |
|--------|--------|--------|------|-------------|
| 500 人 | ~35/s | ~70/s | < 500 MB | 单节点 |
| 2000 人 | ~140/s | ~280/s | ~1 GB | 3 节点集群 |
| 10000 人 | ~700/s | ~1400/s | ~5 GB | 6 节点集群 |

### 2.5 MySQL 读写分离（零代码改动）

直播间管理和在线用户映射表在 2000 人场景下查询量不大，但可配置读写分离预留容量。

```yaml
# 仅改配置，零代码改动
spring:
  datasource:
    # 主库（写）
    url: jdbc:mysql://master:3306/srs_live
    # 从库（读）
    hikari:
      read-only: false
  # 多数据源通过配置层实现，无需改代码
```

**MySQL 容量估算：**

| 并发数 | 写 QPS | 读 QPS | 说明 |
|--------|--------|--------|------|
| 500 人 | < 10/s | < 50/s | 无明显压力 |
| 2000 人 | < 20/s | < 100/s | 单节点足够 |
| 10000 人 | < 50/s | < 300/s | 建议读写分离 |

### 2.6 连接池与线程池调优（仅改配置）

```yaml
# application.yml — 仅调整参数，不改代码
spring:
  datasource:
    hikari:
      maximum-pool-size: 20          # 500人默认；2000人 → 50
      minimum-idle: 5                # 500人默认；2000人 → 10
      connection-timeout: 5000
      max-lifetime: 600000

# WebSocket 线程池
srs:
  websocket:
    boss-threads: 1                   # 保持默认
    worker-threads: 8                 # 500人默认；2000人 → 16
    max-frame-size: 65536
    idle-timeout: 60000

# 心跳扫描任务
  cluster:
    health-check-interval: 10000      # 2000人可调整为 15000
    heartbeat-timeout: 45000
```

**各并发阶段推荐配置速查：**

| 参数 | 500 人 | 2000 人 | 10000 人 |
|------|--------|---------|----------|
| DB 连接池 | 20 | 50 | 100 |
| WS worker 线程 | 8 | 16 | 32 |
| 心跳扫描间隔 | 10s | 15s | 20s |
| Redis 连接池 | 8 | 16 | 32 |

### 2.7 引入消息队列削峰（不改逻辑，新增组件）

在 2000 人并发下，心跳写入 Redis 的 QPS 仅 ~140/s，**尚未达到消息队列的引入门槛**。但当后续扩容至万人级时（~700/s），可引入轻量消息队列削峰。

**引入时机：** 并发 > 5000 人时才需引入，**当前阶段跳过**。

```yaml
# 预留配置（未来启用，不修改现有代码）
srs:
  mq:
    enabled: false                     # 当前关闭，未来改为 true
    type: redis-stream                 # Redis Stream，复用现有 Redis
    # type: rocketmq                   # 或独立 RocketMQ
```

### 2.8 CDN 回源（不改逻辑，新增网络层）

2000 人拉流消耗 **3 Gbps 下行带宽**，直接通过 SRS 节点分发对服务器出口带宽压力较大。引入 CDN 后，观众从 CDN 边缘节点拉流，SRS 仅需维持到 CDN 的回源带宽。

**改动范围：** 网络层配置，**业务代码零改动**。

```
传统模式（无 CDN）：
  主播 → SRS 节点 → 2000 观众（3 Gbps 出口）

CDN 模式：
  主播 → SRS 节点 → CDN 源站 → CDN 边缘节点 → 2000 观众（SRS 出口仅需 50 Mbps 回源带宽）
```

**CDN 引入时机：**

| 并发数 | 下行带宽 | CDN 必要性 | 月成本估算 |
|--------|----------|-----------|-----------|
| 500 人 | ~750 Mbps | 可选 | ~2000 元 |
| 2000 人 | ~3 Gbps | 推荐 | ~8000 元 |
| 10000 人 | ~15 Gbps | 强制 | ~40000 元 |

---

## 三、分阶段扩容方案

### 3.1 阶段一：500 人并发（最小部署）

**资源清单：**

| 组件 | 规格 | 数量 | 说明 |
|------|------|------|------|
| SRS 节点 | 2C4G | 2 | 单节点承载 250 拉流，互备 |
| WS Node | 2C4G | 2 | 单节点承载 500 连接，互备 |
| Redis | 2C4G | 1 | 单节点，无需集群 |
| MySQL | 2C4G | 1 | 单节点 |
| Nginx | 1C2G | 1 | 负载均衡 |

**总资源：** 约 7 台轻量云服务器

**配置改动：** 无（使用默认配置）

### 3.2 阶段二：2000 人并发（配置调优 + 扩容）

**资源清单：**

| 组件 | 规格 | 数量 | 说明 |
|------|------|------|------|
| SRS 节点 | 4C8G | 4 | 单节点承载 500 拉流 |
| WS Node | 4C8G | 2 | 单节点承载 2000 连接 |
| Redis | 4C8G | 3 | 3 节点集群 |
| MySQL | 4C8G | 2 | 1 主 1 从 |
| Nginx | 2C4G | 2 | 双节点 + Keepalived |

**总资源：** 约 13 台云服务器

**配置改动（零代码）：**

```yaml
# 仅修改以下配置项
spring:
  datasource:
    hikari:
      maximum-pool-size: 50
  redis:
    cluster:
      nodes: [3 个节点地址]

srs:
  websocket:
    worker-threads: 16
  cluster:
    health-check-interval: 15000
```

### 3.3 阶段三：10000 人并发（引入 CDN + 消息队列）

**资源清单：**

| 组件 | 规格 | 数量 | 说明 |
|------|------|------|------|
| SRS 节点 | 8C16G | 6 | 单节点承载 1000+ 拉流 |
| WS Node | 8C16G | 4 | 单节点承载 5000 连接 |
| Redis | 8C16G | 6 | 6 节点集群 |
| MySQL | 8C16G | 3 | 1 主 2 从 |
| CDN | 按量 | 1 | 主流 CDN 厂商 |
| MQ (可选) | 4C8G | 3 | RocketMQ 或 Redis Stream |

**配置改动：** 启用 CDN 回源 + 可选启用消息队列

---

## 四、扩容前后对比

### 4.1 500 人 vs 2000 人对比

| 维度 | 500 人 | 2000 人 | 改动量 |
|------|--------|---------|--------|
| SRS 节点数 | 2 | 4 | 新增实例，不改代码 |
| WS 节点数 | 2 | 2 | 无需增加 |
| Redis 集群 | 单节点 | 3 节点 | 改配置，不改代码 |
| MySQL | 单节点 | 1 主 1 从 | 改配置，不改代码 |
| 连接池大小 | 20 | 50 | 改配置，不改代码 |
| WS 线程数 | 8 | 16 | 改配置，不改代码 |
| **代码改动** | **0 行** | **0 行** | **零代码改动** |

### 4.2 成本估算

| 成本项 | 500 人 | 2000 人 | 说明 |
|--------|--------|---------|------|
| 服务器 (月) | ~3000 元 | ~8000 元 | 按云服务器标准价格 |
| 带宽 (月) | ~2000 元 | ~8000 元 | 按 CDN 或 BGP 带宽 |
| Redis (月) | ~500 元 | ~1500 元 | 含在服务器中 |
| **总计** | **~5000 元/月** | **~16000 元/月** | 不含 CDN 则为带宽直连 |

---

## 五、性能基准测试建议

### 5.1 压测指标

| 指标 | 目标值 | 压测方法 |
|------|--------|----------|
| WebSocket 连接数 | 单节点 5000+ | 使用 ws 压测工具建立连接 |
| WebSocket 消息吞吐 | 单节点 5000 msg/s | 模拟心跳 + 房间广播 |
| SRS 拉流并发 | 单节点 500+ | 使用 ffmpeg 模拟 WHEP 拉流 |
| SRS 推流并发 | 单节点 100+ | 使用 ffmpeg 模拟 WHIP 推流 |
| API 响应时间 | P99 < 200ms | 使用 JMeter 压测 REST API |

### 5.2 压测工具推荐

| 场景 | 工具 | 命令示例 |
|------|------|----------|
| WebSocket 压测 | `ws` (npm) | `ws --concurrent 2000 --interval 15s ws://host:8081/ws` |
| SRS 推流压测 | `ffmpeg` | `ffmpeg -re -i test.flv -c copy -f flv rtmp://srs-host/live/stream` |
| SRS 拉流压测 | `ffmpeg` | `ffmpeg -i webrtc://srs-host/live/stream -f null -` |
| HTTP API 压测 | `JMeter` | 线程组 200, Ramp-up 10s |
| 全链路压测 | 自研脚本 | 模拟主播推流 + 观众拉流 + WS 心跳 |

### 5.3 压测通过标准

| 指标 | 通过 | 告警 | 失败 |
|------|------|------|------|
| WS 连接成功率 | > 99% | < 95% | < 90% |
| 推流成功率 | > 98% | < 95% | < 90% |
| 拉流成功率 | > 95% | < 90% | < 85% |
| API P99 响应 | < 200ms | > 500ms | > 1s |
| 内存使用率 | < 70% | > 80% | > 90% |
| CPU 使用率 | < 60% | > 80% | > 90% |

---

## 六、常见问题与应对

### 6.1 "2000 人同时在线，服务器够吗？"

**答：够。** 2000 人 WebSocket 连接仅需 2 台 4C8G 的 WS 节点，SRS 拉流 4 台 4C8G 节点即可。瓶颈不在服务器数量，而在网络出口带宽。2000 人拉流约需 3 Gbps 下行带宽，建议使用 CDN 或大带宽服务器。

### 6.2 "扩容需要改代码吗？"

**答：不需要。** 当前架构已设计为无状态、配置驱动。扩容仅需：
- 新增服务器实例
- 修改 `application.yml` 中的连接池大小、线程数等配置
- 修改 Redis 连接地址为集群模式

### 6.3 "从 500 人到 2000 人，需要重构架构吗？"

**答：不需要重构。** 当前架构中：
- WebSocket 已基于 uid 无状态化
- 集群已基于 Redis PubSub 跨节点广播
- SRS 已支持多节点管理
- 所有组件均可水平扩展

从 500 人到 2000 人，**只需增加节点数量 + 调优配置参数**，不改一行业务代码。

### 6.4 "什么时候需要引入 CDN？"

**建议阈值：** 当并发观众数超过 500 人，或下行带宽超过 1 Gbps 时，建议引入 CDN。CDN 可将 SRS 节点的出口带宽降低 90% 以上，同时观众拉流延迟更低。

### 6.5 "Redis 单节点够用吗？"

**够用评估：** 2000 人场景下，Redis 写 QPS 约 140/s，读 QPS 约 280/s，内存占用 < 1 GB，单节点 4C8G 完全够用。升级到集群模式主要是为了后续万人级扩容预留，**当前阶段可以跳过**。

---

## 七、总结

### 7.1 核心结论

| 问题 | 结论 |
|------|------|
| 500 人并发需要改代码？ | **不需要**，使用默认配置即可 |
| 2000 人并发需要改代码？ | **不需要**，仅需改配置 + 加节点 |
| 万人级需要改代码？ | **不需要改业务逻辑**，仅需加 CDN + MQ 等基础设施组件 |
| 扩容成本？ | 500 人约 5000 元/月，2000 人约 16000 元/月 |
| 扩容时间？ | 新增节点从部署到上线约 30 分钟 |

### 7.2 扩容三步走

```
第一步：加节点（30 分钟）
  → 增加 SRS 节点 + WS 节点，零代码

第二步：调配置（10 分钟）
  → 修改 application.yml 中的连接池、线程数、Redis 集群地址

第三步：加网络设施（按需）
  → 引入 CDN 降低带宽成本
  → 可选引入消息队列支撑万人级
```